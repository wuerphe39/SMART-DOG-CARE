# AI 기반 반응형 Dog Care 시스템

대학 졸업작품 프로젝트 명세서 (현실적 범위 조정본)

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | AI 기반 반응형 Dog Care 시스템 |
| 목표 | 강아지의 기본 행동 상태를 AI로 감지하여 보호자에게 알림 및 케어 제공 |
| 개발 환경 | Windows 11 |
| 개발 기간 | 약 6개월 (기획 1개월 → 개발 4개월 → 테스트/발표 1개월) |
| 개발 형태 | 데스크톱 애플리케이션 + AI 백엔드 |

### 배경 및 필요성

반려견 인구가 꾸준히 증가하면서 보호자가 자리를 비운 사이 강아지의 상태를 실시간으로 파악하려는 수요가 높아지고 있다. 국내외 상용 제품(Furbo, Petcube, 샤오미 펫 카메라 등)은 단순 모션 감지 및 짖음 알림 수준에 머무른다. 본 프로젝트는 YOLOv8 기반 행동 감지와 간단한 음성 감지를 결합하여 보호자에게 의미 있는 상태 정보를 제공하는 PC 기반 시스템을 구현한다.

### 업계 현황 및 차별점

| 제품 | 주요 기능 | 한계 |
|------|-----------|------|
| Furbo (미국) | 짖음 감지, 간식 투척, 양방향 음성 | AI 행동 분석 없음 |
| Petcube (미국) | 모션 감지, 짖음 알림, 레이저 | 행동 상태 구분 없음 |
| 샤오미 펫 카메라 (중국) | 모션 트래킹, 녹화 | AI 분석 없음 |
| 본 프로젝트 | 행동 상태 감지 + 짖음 감지 + 알림 | 하드웨어 없이 PC 소프트웨어 중심 |

---

## 2. 시스템 아키텍처

```
[카메라/마이크 입력]
        |
[데이터 전처리 모듈]  <-- 영상·음성 캡처 (OpenCV, PyAudio)
        |
[AI 분석 엔진]
  |-- 행동 감지 모델  (YOLOv8 - Roboflow 학습 또는 API)
  +-- 짖음 감지 모듈  (librosa 에너지 임계값 기반)
        |
[케어 판단 로직]  <-- 규칙 기반 판단 (임계값 + 상태 지속 시간)
        |
[케어 실행 모듈]
  |-- 알림 전송 (PC 토스트 + 이메일)
  |-- 급식기 제어 Mock (실제 하드웨어 없을 경우 시뮬레이션)
  +-- 진정 사운드 재생 (pygame)
        |
[대시보드 UI]  <-- PyQt6 기반 Windows 앱
```

---

## 3. 핵심 기능

### 3-1. AI 행동 감지 (핵심)

YOLOv8을 사용하며, AI 학습 방식은 두 가지 중 선택한다.

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| Roboflow 직접 학습 | 강아지 영상 직접 촬영 후 라벨링, Roboflow에서 YOLOv8 학습 | 커스텀 모델, 졸업작품 완성도 높음 | 데이터 수집·라벨링 시간 소요 |
| Roboflow Universe 사전학습 모델 | Roboflow Universe에 공개된 강아지 탐지 모델을 다운로드하여 fine-tuning | 데이터 수집 시간 절약, 빠른 시작 | 라벨 카테고리가 다를 수 있어 추가 조정 필요 |
| Vision LLM API | GPT-4o 또는 Claude API에 프레임 이미지를 전송하여 행동 상태 분석 | 구현 간단, 추가 학습 불필요 | API 호출 비용 발생, 실시간 처리 속도 제한 (1~2 FPS 수준) |

> AWS Rekognition / Google Vision API는 일반 객체 탐지 용도이며, `resting/active/near_bowl` 같은 강아지 행동 상태를 직접 반환하지 않으므로 적합하지 않음.

감지 가능한 행동 상태 (현실적 범위):

| 행동 상태 | 설명 | 케어 액션 |
|-----------|------|-----------|
| resting | 장시간 누워 거의 움직이지 않음 | 로그 기록 |
| active | 활발하게 움직임 | 로그 기록 |
| near_bowl | 밥그릇 또는 물그릇 앞에 머무름 | 급식 스케줄 체크 알림 |
| absent | 일정 시간 이상 화면에서 감지 안됨 | 보호자 알림 |

> 제외 항목: 꼬리 흔들기, 배 긁기, 핥기, 배변 감지 등 마이크로 행동은 현재 기술 수준 및 6개월 개발 기간 내 구현 불가 판단으로 제외. 상용 제품(Furbo, Petcube)도 해당 기능을 제공하지 않음.

### 3-2. 짖음 감지

- librosa 기반 오디오 에너지 임계값으로 짖음 유무 판단
- 짖음 감지 시 보호자 알림 발송
- 진정 사운드 자동 재생 (선택)

> 제외 항목: 짖음 유형 분류(경보형/분리불안형/놀이형)는 연구 수준의 과제로 6개월 내 구현 불가. 음원 분리(노이즈 필터링)도 DSP 전문 지식이 필요하여 제외.

### 3-3. 보호자 대시보드 (Windows 앱)

- 실시간 카메라 피드 + 행동 상태 오버레이 표시
- 일별 행동 상태 통계 차트
- 케어 이력 로그 뷰어
- 진정 사운드 / 급식 Mock 수동 제어

### 3-4. 알림 시스템

- 상태별 알림 등급: 정보 / 주의 / 경고
- 보호자 PC 토스트 알림 (winotify 또는 plyer, Windows 11 호환)
- 이메일 전송 (smtplib, Gmail 사용 시 앱 비밀번호 설정 필요)

---

## 4. 기술 스택

### 백엔드 / AI

| 분류 | 기술 |
|------|------|
| 언어 | Python 3.11 |
| AI 학습 방식 | Roboflow 직접 학습 또는 외부 API 활용 (개발 진행 중 결정) |
| 객체 탐지 | YOLOv8 (Ultralytics) |
| 음성 처리 | librosa, PyAudio |
| 데이터 관리 | SQLite3 |

### 프론트엔드 (Windows 앱)

| 분류 | 기술 |
|------|------|
| UI 프레임워크 | PyQt6 |
| 영상 처리 | OpenCV (cv2) |
| 차트 | matplotlib |
| 사운드 재생 | pygame |
| 알림 | winotify 또는 plyer (Windows 11 호환 토스트) |

### 개발 도구

| 분류 | 기술 |
|------|------|
| IDE | VS Code / PyCharm |
| 버전 관리 | Git + GitHub |
| 가상환경 | Python venv |
| 패키지 관리 | pip + requirements.txt |
| 문서화 | Markdown |

---

## 5. 디렉토리 구조

```
dog-care-ai/
├── main.py                    # 앱 진입점
├── requirements.txt
├── README.md
├── project.md
│
├── config/
│   ├── settings.yaml          # 앱 설정 (카메라 ID, 알림 등)
│   └── care_rules.yaml        # 케어 규칙 정의 (상태별 임계값)
│
├── ai/
│   ├── behavior_detector.py   # 행동 감지 (YOLOv8)
│   ├── audio_detector.py      # 짖음 감지 (librosa)
│   └── models/                # 학습된 모델 파일 (.pt)
│
├── care/
│   ├── care_engine.py         # 케어 판단 로직
│   ├── notifier.py            # 알림 전송 (토스트, 이메일)
│   ├── feeder_mock.py         # 급식기 Mock 제어
│   └── sound_player.py        # 진정 사운드 재생
│
├── ui/
│   ├── main_window.py         # 메인 윈도우 (PyQt6)
│   ├── dashboard.py           # 대시보드 위젯
│   ├── camera_view.py         # 실시간 카메라 뷰
│   └── assets/                # 아이콘, 이미지, 사운드 파일
│
├── data/
│   ├── database.py            # SQLite 관리
│   ├── logs/                  # 케어 이력 로그
│   └── recordings/            # 이벤트 영상 저장
│
└── tests/
    ├── test_behavior.py
    ├── test_audio.py
    └── test_care_engine.py
```

---

## 6. 개발 일정 (6개월)

### Phase 1 — 기획 및 환경 구성 (1개월)

- [ ] 요구사항 정의 및 기능 범위 확정
- [ ] 기술 스택 선정 및 개발 환경 세팅 (Windows)
- [ ] AI 학습 방식 결정 (Roboflow 직접 학습 vs 외부 API)
- [ ] 시스템 아키텍처 설계

### Phase 2 — AI 모델 개발 (2개월)

- [ ] 강아지 행동 데이터셋 수집 (공개 데이터셋 + 자체 촬영)
- [ ] Roboflow에서 라벨링 및 YOLOv8 학습 (또는 API 연동)
- [ ] 짖음 감지 모듈 개발 (librosa 에너지 임계값)
- [ ] 모델 성능 평가 및 임계값 조정

### Phase 3 — 백엔드 / 케어 로직 개발 (1개월)

- [ ] 케어 판단 엔진 구현 (상태 지속 시간 기반 규칙)
- [ ] SQLite 데이터베이스 설계 및 구현
- [ ] 알림 시스템 구현 (Windows 토스트, 이메일)
- [ ] 급식기 Mock 모듈 구현

### Phase 4 — UI 개발 및 통합 (1개월)

- [ ] PyQt6 메인 윈도우 구현
- [ ] 실시간 카메라 피드 + 행동 상태 오버레이
- [ ] 대시보드 통계 차트 구현
- [ ] 전체 모듈 통합 테스트

### Phase 5 — 테스트 및 발표 준비 (1개월)

- [ ] 실제 강아지 환경에서 통합 테스트
- [ ] 버그 수정 및 성능 최적화
- [ ] 발표 자료 (PPT) 및 시연 영상 제작
- [ ] 최종 코드 정리 및 문서화

---

## 7. 데이터셋

### 공개 데이터셋

| 데이터셋 | 내용 | 출처 |
|----------|------|------|
| Dog Detection Dataset | 강아지 탐지용 YOLOv8 모델 및 이미지 | Roboflow Universe |
| Open Images Dataset (dog) | 강아지 포함 이미지 대량 | Google Open Images |
| 자체 수집 | 직접 촬영 영상, resting/active/near_bowl 라벨링 | — |

> Stanford Dogs Dataset은 품종 분류(Breed Classification)용 데이터셋으로 행동 감지에 사용 불가하여 제외.

### 행동 라벨 카테고리

`resting` / `active` / `near_bowl` / `absent`

---

## 8. 평가 기준

| 평가 항목 | 목표 수치 | 근거 |
|-----------|-----------|------|
| 행동 감지 정확도 | 70% 이상 | 단순 4개 상태 분류 기준 |
| 짖음 감지 정확도 | 80% 이상 | 에너지 임계값 기반 |
| 실시간 처리 속도 | 15 FPS 이상 | YOLOv8n 기준 일반 노트북 |
| 알림 지연 시간 | 이벤트 발생 후 5초 이내 | |
| 코드 품질 | 모듈화, 주석, README 완비 | |

---

## 9. 향후 확장 가능성 (졸업 이후)

- 실제 자동 급식기 하드웨어 연동 (Arduino / Raspberry Pi)
- 모바일 앱 연동 (React Native + Firebase)
- 다견 환경 지원 (개체 구분 추적)
- 클라우드 동기화 및 다중 기기 접근
- 수의사 리포트 자동 생성

---

## 10. 현실성 검토 요약

| 항목 | 원안 | 조정안 | 이유 |
|------|------|--------|------|
| 꼬리 흔들기 감지 | 포함 | 제외 | 상용 제품도 미지원, 구현 난이도 매우 높음 |
| 배 긁기 / 핥기 감지 | 포함 | 제외 | 마이크로 행동 감지는 연구 수준 |
| 배변 감지 | 포함 | 제외 | 전용 데이터셋 없음, 6개월 내 불가 |
| 짖음 유형 분류 | 포함 | 제외 | 학술 연구 수준, 상용도 미구현 |
| 노이즈 필터링 | 포함 | 제외 | DSP 전문 지식 필요 |
| MediaPipe Pose | 포함 | 제외 | 사람 전용 모델, 개에 미적용 |
| 감정 분류 CNN 직접 학습 | 포함 | 조정 | Roboflow 사전학습 모델 또는 API 활용으로 대체 |
| 급식기 하드웨어 제어 | 포함 | Mock으로 대체 | 실제 하드웨어 없을 경우 Mock 구현 |
| 정확도 목표 | 85% | 70% | 단순화된 행동 상태 기준 현실적 수치 |
| AWS/Google Vision API | 외부 API 활용 | Roboflow Universe 사전학습 모델 또는 Vision LLM API로 대체 | 해당 API는 강아지 행동 상태 미지원 |
| win10toast | 알림 라이브러리 | winotify / plyer로 교체 | Windows 11 환경에서 win10toast 불안정 |
| Stanford Dogs Dataset | 데이터셋 | 제외 | 품종 분류용이며 행동 감지 목적과 불일치 |
